# Copy this file to .env.local and fill in your real key.
# .env.local is gitignored and must NEVER be committed.
# Get a key from https://console.anthropic.com (separate from your Claude subscription).
# Set a monthly spend cap in the console before deploying anything public.

ANTHROPIC_API_KEY=your-key-here
